#!/bin/bash

## Use 'fin develop' instead
##
## init is here for compliance

# Start containers
fin project reset -f

echo "Preparing to install dependencies..."
cd "$PROJECT_ROOT/$DOCROOT"
rm -f "$PROJECT_ROOT/$DOCROOT/node_modules" 2>/dev/null
fin exec npm install

fin develop
